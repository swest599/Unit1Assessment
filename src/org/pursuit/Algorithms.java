package org.pursuit;

public class Algorithms implements DataStructures {

    public Algorithms() {
        super(stringToCharArray, multiplicationTableList, wordLength, uniqueCharacters);
        @Override{

            stringToCharArray();
            char[] chars;
            for (char r: word) {
                chars.add(r);
                System.out.println(chars);}
        }
        @Override
            multiplicationTableList();
            for (int i =0; i < 10; i++){
                i *= number;
                arr.add(number);
                System.out.println(arr);
            }

        @Override {
            wordLength();
            for (int i = 0; i < yada.size(); i++) {
                yada.put(wordArray[i],wordArray[i].size);
            }
            System.out.println(yada);
        }
        @Override{
            uniqueCharacters();

        }
        }



        }




